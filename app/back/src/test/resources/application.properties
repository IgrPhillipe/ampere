spring.profiles.active=test

spring.datasource.url=${TEST_DATABASE_URL:jdbc:postgresql://localhost:5432/ampere_test}
spring.datasource.username=${TEST_DATABASE_USER:ampere}
spring.datasource.password=${TEST_DATABASE_PASSWORD:ampere}

spring.jpa.hibernate.ddl-auto=create-drop

# Substitui o de main/resources: o que muda comportamento precisa ser repetido aqui.
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# Segredo proprio dos testes, para nao herdar o de desenvolvimento.
ampere.jwt.secret=segredo-de-teste-com-pelo-menos-32-caracteres
ampere.jwt.expiration=1h
