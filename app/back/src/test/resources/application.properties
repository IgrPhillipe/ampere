spring.profiles.active=test

spring.datasource.url=${TEST_DATABASE_URL:jdbc:postgresql://localhost:5432/ampere_test}
spring.datasource.username=${TEST_DATABASE_USER:ampere}
spring.datasource.password=${TEST_DATABASE_PASSWORD:ampere}

spring.jpa.hibernate.ddl-auto=create-drop

# Este arquivo substitui o de main/resources, entao o que muda comportamento
# precisa ser repetido aqui: sem isto o teste roda com a sessao aberta na
# serializacao e nao ve os problemas de lazy loading que producao veria.
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
